@startuml
start
:User executes command;

if () then ([command is valid])
    if () then ([index of group in model])
        :Delete group from model;
    else ([else])
    endif

else([else])
endif
stop
@enduml
