package seedu.address.ui;

import javafx.stage.Stage;

/**
 * API of UI component
 */
public interface Ui {

    /**
     * Starts the UI (and the App).
     *
     * @param primaryStage Main javaFx container.
     */
    void start(Stage primaryStage);

}
