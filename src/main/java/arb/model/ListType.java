package arb.model;

/**
 * Enum to represent the type of list currently being shown to the user and the
 * type of list a command should show to the user.
 */
public enum ListType {
    NONE, PROJECT, CLIENT, TAG
}
