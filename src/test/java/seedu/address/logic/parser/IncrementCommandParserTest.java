package seedu.address.logic.parser;

public class IncrementCommandParserTest {

    //assert failure with missing descriptor

    //ensure that increment does indeed increment the count

    //throw error message when there is a negative input

    //throw error message with zero input

    //throw error message when input exceed limit

    //throw error message when a bunch of random strings are the input
}
